"""
companion package initializer.

This package contains the modules that implement the AI companion loop for the
trading‑lab project.  The AI companion monitors MLflow runs, diagnoses
failures, proposes safe patches, tests them, and optionally re‑runs
impacted jobs.  See individual module docstrings for details on each
component.
"""
